CREATE TABLE `nutrients` (
    `id` INTEGER PRIMARY KEY,
    `units` VARCHAR(7) NULL,
    `tagname` VARCHAR(20) NULL,
    `description` VARCHAR(60) NULL,
    `precision` INTEGER NULL,
    `common_order` INTEGER NULL
);
